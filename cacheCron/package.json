{
  "name": "politopics-cache-cron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/index.js",
    "zip": "node scripts/zip.mjs",
    "build": "npm run clean && npm run bundle && npm run zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.679.0",
    "@keyhole-koro/politopics-notification": "latest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "archiver": "^7.0.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3"
  }
}
