{
  "name": "@politopics/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json && node scripts/prepare-local-dist.mjs && node scripts/zip.mjs",
    "build:local": "tsc -p tsconfig.json && node scripts/prepare-local-dist.mjs && node scripts/zip.mjs",
    "seed": "ts-node src/scripts/seed-localstack.ts",
    "bootstrap:localstack": "npm run build:local && npm --prefix ../../shared/mock install && node ../../shared/mock/upload_articles.js --endpoint http://localstack:4566 --bucket politopics-articles-local"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.679.0",
    "@aws-sdk/client-s3": "^3.679.0",
    "@aws-sdk/lib-dynamodb": "^3.679.0",
    "@fastify/aws-lambda": "^6.1.1",
    "@fastify/cors": "^10.1.0",
    "@keyhole-koro/politopics-notification": "latest",
    "dotenv": "^16.4.7",
    "fastify": "^5.6.2",
    "fastify-plugin": "^5.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/archiver": "^6.0.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.1",
    "archiver": "^7.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.16.2",
    "typescript": "^5.6.3"
  }
}
